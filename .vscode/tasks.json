{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Initialize DuckDB PostgreSQL Connection",
            "type": "shell",
            "command": "${workspaceFolder}/.venv/bin/python ${workspaceFolder}/duckdb_cli.py --init",
            "runOptions": {
                "runOn": "folderOpen"
            },
            "presentation": {
                "reveal": "silent",
                "panel": "shared"
            },
            "group": "none"
        }
    ]
}
